'use strict';

const myName = 'Natalia Migallon Fernandez';

const mySentence = document.querySelector('.sentence');

mySentence.innerHTML = (`El nombre de mi compañera es ${myName} , y está compuesto por ${myName.length} caracteres`);